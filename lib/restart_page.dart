import 'package:flutter/material.dart';

class RestartPage extends StatelessWidget {
  const RestartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Hello RestartPage page");
  }
}
